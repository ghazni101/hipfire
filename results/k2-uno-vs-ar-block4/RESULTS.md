# K2-Horizon Uno vs AR decode — A/B results (gfx1100)

Generated by `scripts/uno_ab_report.py` from `results/k2-uno-vs-ar/{ar,uno}.json`.
Raw per-run JSON and daemon logs sit next to this file.

## Fixtures

| item | value |
|---|---|
| worktree | `/home/ghazni/github/hipfire/k2-uno` @ `4217f03d` |
| GPU (daemon-reported) | `{"type": "diag", "arch": "gfx1100", "hip_version": "7.15", "vram_free_mb": 16536, "vram_total_mb": 24560, "model_loaded": true, "model_arch": "qwen3", "kernels": 0, "kernel_hashes": 0}` |
| model (host) | `/home/ghazni/models/hipfire/IFM/K2-Horizon-7B-MQ4/k2-horizon-7b.mq4` |
| model (as the daemon reports it) | `/models/hipfire/IFM/K2-Horizon-7B-MQ4/k2-horizon-7b.mq4` |
| adapter (Uno side) | `/home/ghazni/models/RAW/IFM/K2-Horizon-7B-Uno/source` |
| prompt file | `benchmarks/prompts/k2_uno_qa_fixed.txt` |
| prompt md5 / chars | `c77d12df7ff421ee819ff76d2bb6abc6` / 2508 |
| prompt tokens | 527 (>= 256 required) |
| max tokens | 128 |
| warmups / runs | 3 / 5 |
| `hipfire` sha256 | `f72a6028d38920149a8c3732d2cf4b23e49e2a0caf4b799202aacf6a2f23f6af` |
| `daemon` sha256 | `c0b25825e67c88caad331af32865c861240dc1a1f922002255c7ad8101325b43` |
| source state | working tree is **dirty** (22 uncommitted paths) by design — the objective forbids committing. The digests above identify the measured build. |

Protocol: `hipfire bench`, greedy (temperature 0), Q8 KV, `--backend noslots --workload stateless --reasoning-on`, a fresh daemon process per side, and `HIPFIRE_BENCH_REPEAT_PENALTY=1.0` on **both** sides so AR and the speculative verifier share one sampling law.

`hipfire bench` drives the model through the native daemon protocol — the same path a `hipfire serve` request takes — and reports decode tok/s over post-prefill generation time only, so prefill is excluded by construction rather than by subtracting a separately-timed phase. `--reasoning-on` is required on both sides: the default answer mode caps thinking at one token, which stops the Uno emitter after 3 tokens while AR runs to 128 and makes the two sides incomparable.

## Decode throughput (post-prefill, generated tokens only)

Generated tokens per run: **128** (AR) / **128** (Uno), both rounding to the 128-token budget. A reasoning turn reports an empty `token_ids`, so the count is recovered from the timing stats the JSON does carry: `wall = g / (ttft + g/decode)`.

| side | median decode tok/s | run list (tok/s) | per-run generation s | tau (median) |
|---|---|---|---|---|
| AR (no draft) | **67.3** | [67.2, 67.1, 67.3, 67.5, 67.3] | [1.9, 1.9, 1.9, 1.9, 1.9] | – |
| Uno (block 4) | **61.5** | [61.5, 61.6, 61.5, 61.6, 61.5] | [2.1, 2.1, 2.1, 2.1, 2.1] | 1.180 |

**Ratio: `61.5 / 67.3` = 0.914x** — the success threshold is 2.000x, so the throughput criterion **FAILS**.

## Token identity (greedy, same prompt)

- AR emitted-stream md5 (all 5 runs): `['ca58558a3c874f08ea6822c0926c6cfb']`
- Uno emitted-stream md5 (all 5 runs): `['ca58558a3c874f08ea6822c0926c6cfb']`
- The digest covers **every** emitted channel — a reasoning turn puts all its tokens on `reasoning`, so a text-only hash would read `md5("")` on both sides and prove nothing. Equal digests here mean an identical token stream, through the full 128-token budget (`temperature 0`, so the run is reproducible).
- **PASS — identical emitted stream on every run**

## Verdict

Throughput criterion **not met** (0.914x < 2.0x); token identity passes.

This is the expected outcome for this adapter and is not a measurement artefact: the same-size conditional-LoRA drafter needs a window to run below `tokens_per_window / 2` AR tokens, and the window is two full target forwards. See `../K2-UNO-THROUGHPUT.md` for the measured arithmetic across every allowed knob value.

_No AR weakening, no shortened generation, no disabled verification, no single-run or probe-only numbers: both sides ran the same protocol above._
